<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.tenant.mapper.CrmTenantMapper">

    <resultMap type="com.ruoyi.crm.tenant.domain.CrmTenant" id="CrmTenantResult">
        <result property="tenantId"   column="tenant_id"/>
        <result property="tenantName" column="tenant_name"/>
        <result property="status"     column="status"/>
        <result property="createBy"   column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy"   column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark"     column="remark"/>
        <result property="version"   column="version"/>
    </resultMap>

    <sql id="selectColumns">
        tenant_id, tenant_name, status, create_by, create_time, update_by, update_time, remark, version
    </sql>

    <select id="selectByTenantId" parameterType="String" resultMap="CrmTenantResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_tenant
        WHERE tenant_id = #{tenantId}
    </select>

    <select id="selectAll" resultMap="CrmTenantResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_tenant
        ORDER BY create_time
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.tenant.domain.CrmTenant">
        INSERT INTO crm_tenant (
            tenant_id, tenant_name, status, create_by, create_time, update_by, update_time, remark, version
        ) VALUES (
            #{tenantId}, #{tenantName}, #{status}, #{createBy}, NOW(), #{updateBy}, NOW(), #{remark}, 0
        )
    </insert>

    <update id="update" parameterType="com.ruoyi.crm.tenant.domain.CrmTenant">
        UPDATE crm_tenant
        SET tenant_name = #{tenantName},
            status = #{status},
            update_by = #{updateBy},
            update_time = NOW(),
            remark = #{remark},
            version = version + 1
        WHERE tenant_id = #{tenantId} AND version = #{version}
    </update>

</mapper>
