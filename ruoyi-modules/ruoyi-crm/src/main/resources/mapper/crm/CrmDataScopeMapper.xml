<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.permission.mapper.CrmDataScopeMapper">
    <select id="countDeptOrDescendant" resultType="int">
        SELECT COUNT(1)
        FROM sys_dept
        WHERE dept_id = #{targetDeptId}
          AND status = '0'
          AND del_flag = '0'
          AND (dept_id = #{operatorDeptId} OR FIND_IN_SET(#{operatorDeptId}, ancestors))
    </select>
</mapper>
