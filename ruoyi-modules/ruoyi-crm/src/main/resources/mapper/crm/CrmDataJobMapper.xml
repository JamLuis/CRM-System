<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.datajob.mapper.CrmDataJobMapper">

    <resultMap type="com.ruoyi.crm.datajob.domain.CrmDataJob" id="CrmDataJobResult">
        <result property="jobId"          column="job_id"/>
        <result property="tenantId"       column="tenant_id"/>
        <result property="jobType"        column="job_type"/>
        <result property="status"         column="status"/>
        <result property="fileName"       column="file_name"/>
        <result property="storageKey"     column="storage_key"/>
        <result property="queryCondition" column="query_condition"/>
        <result property="totalCount"     column="total_count"/>
        <result property="successCount"   column="success_count"/>
        <result property="failedCount"    column="failed_count"/>
        <result property="rowResults"     column="row_results"/>
        <result property="expireTime"     column="expire_time"/>
        <result property="operatorId"     column="operator_id"/>
        <result property="operatorName"   column="operator_name"/>
        <result property="errorMsg"       column="error_msg"/>
        <result property="startTime"      column="start_time"/>
        <result property="finishTime"     column="finish_time"/>
        <result property="version"        column="version"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
    </resultMap>

    <sql id="selectColumns">
        job_id, tenant_id, job_type, status, file_name, storage_key, query_condition,
        total_count, success_count, failed_count, row_results, expire_time,
        operator_id, operator_name, error_msg, start_time, finish_time,
        version, del_flag, create_by, create_time, update_by, update_time
    </sql>

    <select id="selectByJobId" resultMap="CrmDataJobResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_data_job
        WHERE tenant_id = #{tenantId} AND job_id = #{jobId} AND del_flag = '0'
    </select>

    <select id="selectList" resultMap="CrmDataJobResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_data_job
        WHERE tenant_id = #{tenantId} AND del_flag = '0'
        <if test="jobType != null and jobType != ''">
            AND job_type = #{jobType}
        </if>
        ORDER BY create_time DESC
        LIMIT 200
    </select>

    <select id="selectExpiredExports" resultMap="CrmDataJobResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_data_job
        WHERE del_flag = '0' AND job_type = 'EXPORT' AND status = 'SUCCESS'
          AND expire_time IS NOT NULL AND expire_time &lt; #{now}
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.datajob.domain.CrmDataJob">
        INSERT INTO crm_data_job (
            job_id, tenant_id, job_type, status, file_name, storage_key, query_condition,
            total_count, success_count, failed_count, row_results, expire_time,
            operator_id, operator_name, error_msg, start_time, finish_time,
            version, del_flag, create_by, create_time, update_by, update_time
        ) VALUES (
            #{jobId}, #{tenantId}, #{jobType}, #{status}, #{fileName}, #{storageKey}, #{queryCondition},
            #{totalCount}, #{successCount}, #{failedCount}, #{rowResults}, #{expireTime},
            #{operatorId}, #{operatorName}, #{errorMsg}, #{startTime}, #{finishTime},
            #{version}, #{delFlag}, #{createBy}, NOW(), #{updateBy}, NOW()
        )
    </insert>

    <update id="update" parameterType="com.ruoyi.crm.datajob.domain.CrmDataJob">
        UPDATE crm_data_job
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="fileName != null">file_name = #{fileName},</if>
            <if test="storageKey != null">storage_key = #{storageKey},</if>
            <if test="queryCondition != null">query_condition = #{queryCondition},</if>
            <if test="totalCount != null">total_count = #{totalCount},</if>
            <if test="successCount != null">success_count = #{successCount},</if>
            <if test="failedCount != null">failed_count = #{failedCount},</if>
            <if test="rowResults != null">row_results = #{rowResults},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = NOW(),
            version = version + 1
        </set>
        WHERE tenant_id = #{tenantId} AND job_id = #{jobId} AND del_flag = '0'
    </update>

</mapper>
