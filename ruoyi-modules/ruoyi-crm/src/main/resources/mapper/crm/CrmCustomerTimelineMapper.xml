<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.audit.mapper.CrmCustomerTimelineMapper">

    <resultMap type="com.ruoyi.crm.audit.domain.CrmCustomerTimeline" id="CrmCustomerTimelineResult">
        <result property="id"           column="id"/>
        <result property="tenantId"     column="tenant_id"/>
        <result property="customerId"   column="customer_id"/>
        <result property="eventType"    column="event_type"/>
        <result property="eventData"    column="event_data"/>
        <result property="operatorId"   column="operator_id"/>
        <result property="operatorName" column="operator_name"/>
        <result property="createTime"   column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.ruoyi.crm.audit.domain.CrmCustomerTimeline">
        INSERT INTO crm_customer_timeline (
            id, tenant_id, customer_id, event_type, event_data, operator_id, operator_name, create_time
        ) VALUES (
            #{id}, #{tenantId}, #{customerId}, #{eventType}, #{eventData}, #{operatorId}, #{operatorName}, NOW()
        )
    </insert>

    <select id="selectByCustomerId" resultMap="CrmCustomerTimelineResult">
        SELECT id, tenant_id, customer_id, event_type, event_data, operator_id, operator_name, create_time
        FROM crm_customer_timeline
        WHERE tenant_id = #{tenantId} AND customer_id = #{customerId}
        ORDER BY create_time DESC
    </select>

</mapper>
