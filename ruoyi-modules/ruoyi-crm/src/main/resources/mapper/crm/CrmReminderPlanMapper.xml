<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.followup.mapper.CrmReminderPlanMapper">

    <resultMap type="com.ruoyi.crm.followup.domain.CrmReminderPlan" id="CrmReminderPlanResult">
        <result property="planId"               column="plan_id"/>
        <result property="tenantId"             column="tenant_id"/>
        <result property="customerId"           column="customer_id"/>
        <result property="sourceFollowUpId"     column="source_follow_up_id"/>
        <result property="planKey"               column="plan_key"/>
        <result property="plannedFollowUpAt"    column="planned_follow_up_at"/>
        <result property="scheduledAt"           column="scheduled_at"/>
        <result property="status"                column="status"/>
        <result property="createBy"              column="create_by"/>
        <result property="createTime"            column="create_time"/>
        <result property="updateBy"              column="update_by"/>
        <result property="updateTime"            column="update_time"/>
        <result property="version"               column="version"/>
        <result property="delFlag"               column="del_flag"/>
    </resultMap>

    <sql id="selectColumns">
        plan_id, tenant_id, customer_id, source_follow_up_id, plan_key,
        planned_follow_up_at, scheduled_at, status,
        create_by, create_time, update_by, update_time, version, del_flag
    </sql>

    <select id="selectByPlanId" resultMap="CrmReminderPlanResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_reminder_plan
        WHERE tenant_id = #{tenantId} AND plan_id = #{planId} AND del_flag = '0'
    </select>

    <select id="selectActiveByCustomer" resultMap="CrmReminderPlanResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_reminder_plan
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
        LIMIT 1
    </select>

    <select id="selectByCustomer" resultMap="CrmReminderPlanResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_reminder_plan
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND del_flag = '0'
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.followup.domain.CrmReminderPlan">
        INSERT INTO crm_reminder_plan (
            plan_id, tenant_id, customer_id, source_follow_up_id, plan_key,
            planned_follow_up_at, scheduled_at, status,
            create_by, create_time, update_by, update_time, version, del_flag
        ) VALUES (
            #{planId}, #{tenantId}, #{customerId}, #{sourceFollowUpId}, #{planKey},
            #{plannedFollowUpAt}, #{scheduledAt}, #{status},
            #{createBy}, NOW(), #{updateBy}, NOW(), 0, '0'
        )
    </insert>

    <update id="cancelActiveByCustomer">
        UPDATE crm_reminder_plan
        SET status = 'CANCELLED',
            update_by = #{updateBy},
            update_time = NOW(),
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </update>

    <update id="cancelByPlanId">
        UPDATE crm_reminder_plan
        SET status = 'CANCELLED',
            update_by = #{updateBy},
            update_time = NOW(),
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND plan_id = #{planId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </update>

</mapper>
