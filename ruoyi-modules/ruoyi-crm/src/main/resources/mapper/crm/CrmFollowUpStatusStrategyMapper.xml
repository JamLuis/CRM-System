<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.followup.mapper.CrmFollowUpStatusStrategyMapper">

    <resultMap type="com.ruoyi.crm.followup.domain.CrmFollowUpStatusStrategy" id="CrmFollowUpStatusStrategyResult">
        <result property="strategyId"              column="strategy_id"/>
        <result property="tenantId"                 column="tenant_id"/>
        <result property="insufficientThreshold"   column="insufficient_threshold"/>
        <result property="severeThreshold"          column="severe_threshold"/>
        <result property="effectiveFrom"            column="effective_from"/>
        <result property="status"                    column="status"/>
        <result property="createBy"                  column="create_by"/>
        <result property="createTime"               column="create_time"/>
        <result property="updateBy"                  column="update_by"/>
        <result property="updateTime"                column="update_time"/>
        <result property="version"                   column="version"/>
        <result property="delFlag"                   column="del_flag"/>
    </resultMap>

    <sql id="selectColumns">
        strategy_id, tenant_id, insufficient_threshold, severe_threshold,
        effective_from, status,
        create_by, create_time, update_by, update_time, version, del_flag
    </sql>

    <select id="selectActive" resultMap="CrmFollowUpStatusStrategyResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_follow_up_status_strategy
        WHERE tenant_id = #{tenantId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
        ORDER BY effective_from DESC
        LIMIT 1
    </select>

    <select id="selectByStrategyId" resultMap="CrmFollowUpStatusStrategyResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_follow_up_status_strategy
        WHERE tenant_id = #{tenantId}
          AND strategy_id = #{strategyId}
          AND del_flag = '0'
    </select>

    <select id="selectAll" resultMap="CrmFollowUpStatusStrategyResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_follow_up_status_strategy
        WHERE tenant_id = #{tenantId} AND del_flag = '0'
        ORDER BY effective_from DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.followup.domain.CrmFollowUpStatusStrategy">
        INSERT INTO crm_follow_up_status_strategy (
            strategy_id, tenant_id, insufficient_threshold, severe_threshold,
            effective_from, status,
            create_by, create_time, update_by, update_time, version, del_flag
        ) VALUES (
            #{strategyId}, #{tenantId}, #{insufficientThreshold}, #{severeThreshold},
            #{effectiveFrom}, #{status},
            #{createBy}, NOW(), #{updateBy}, NOW(), 0, '0'
        )
    </insert>

    <update id="deactivateOld">
        UPDATE crm_follow_up_status_strategy
        SET status = 'INACTIVE',
            update_by = #{updateBy},
            update_time = NOW(),
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND strategy_id != #{excludeStrategyId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </update>

</mapper>
