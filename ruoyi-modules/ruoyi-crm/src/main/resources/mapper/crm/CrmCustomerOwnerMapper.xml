<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.customer.mapper.CrmCustomerOwnerMapper">

    <resultMap type="com.ruoyi.crm.customer.domain.CrmCustomerOwner" id="CrmCustomerOwnerResult">
        <result property="id"          column="id"/>
        <result property="tenantId"    column="tenant_id"/>
        <result property="customerId"  column="customer_id"/>
        <result property="userId"     column="user_id"/>
        <result property="userName"    column="user_name"/>
        <result property="roleType"    column="role_type"/>
        <result property="status"      column="status"/>
        <result property="createBy"    column="create_by"/>
        <result property="createTime"  column="create_time"/>
        <result property="updateBy"    column="update_by"/>
        <result property="updateTime"  column="update_time"/>
        <result property="version"     column="version"/>
        <result property="delFlag"     column="del_flag"/>
    </resultMap>

    <sql id="selectColumns">
        id, tenant_id, customer_id, user_id, user_name, role_type, status,
        create_by, create_time, update_by, update_time, version, del_flag
    </sql>

    <select id="selectActiveByCustomer" resultMap="CrmCustomerOwnerResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_customer_owner
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
        ORDER BY role_type DESC, create_time
    </select>

    <select id="selectActivePrimary" resultMap="CrmCustomerOwnerResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_customer_owner
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND role_type = 'PRIMARY'
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </select>

    <select id="selectActiveCollaborators" resultMap="CrmCustomerOwnerResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_customer_owner
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND role_type = 'COLLABORATOR'
          AND status = 'ACTIVE'
          AND del_flag = '0'
        ORDER BY create_time
    </select>

    <select id="selectByUserAsPrimary" resultMap="CrmCustomerOwnerResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_customer_owner
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND role_type = 'PRIMARY'
          AND status = 'ACTIVE'
          AND del_flag = '0'
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserAsCollaborator" resultMap="CrmCustomerOwnerResult">
        SELECT <include refid="selectColumns"/>
        FROM crm_customer_owner
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND role_type = 'COLLABORATOR'
          AND status = 'ACTIVE'
          AND del_flag = '0'
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.customer.domain.CrmCustomerOwner">
        INSERT INTO crm_customer_owner (
            id, tenant_id, customer_id, user_id, user_name, role_type, status,
            create_by, create_time, update_by, update_time, version, del_flag
        ) VALUES (
            #{id}, #{tenantId}, #{customerId}, #{userId}, #{userName}, #{roleType}, #{status},
            #{createBy}, NOW(), #{updateBy}, NOW(), 0, '0'
        )
    </insert>

    <update id="deactivateByCustomerAndRole">
        UPDATE crm_customer_owner
        SET status = 'INACTIVE',
            update_by = #{updateBy},
            update_time = NOW(),
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND role_type = #{roleType}
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </update>

    <update id="deactivateCollaborator">
        UPDATE crm_customer_owner
        SET status = 'INACTIVE',
            update_by = #{updateBy},
            update_time = NOW(),
            version = version + 1
        WHERE tenant_id = #{tenantId}
          AND customer_id = #{customerId}
          AND user_id = #{userId}
          AND role_type = 'COLLABORATOR'
          AND status = 'ACTIVE'
          AND del_flag = '0'
    </update>

</mapper>
