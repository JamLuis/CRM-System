<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.crm.common.mapper.CrmIdempotencyKeyMapper">

    <resultMap type="com.ruoyi.crm.common.domain.CrmIdempotencyKey" id="CrmIdempotencyKeyResult">
        <result property="id"             column="id"/>
        <result property="tenantId"       column="tenant_id"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="requestHash"    column="request_hash"/>
        <result property="responseStatus" column="response_status"/>
        <result property="responseBody"   column="response_body"/>
        <result property="expireTime"     column="expire_time"/>
        <result property="createTime"     column="create_time"/>
    </resultMap>

    <select id="selectByKey" resultMap="CrmIdempotencyKeyResult">
        SELECT id, tenant_id, idempotency_key, request_hash, response_status, response_body, expire_time, create_time
        FROM crm_idempotency_key
        WHERE tenant_id = #{tenantId} AND idempotency_key = #{idempotencyKey}
          AND (expire_time IS NULL OR expire_time &gt; NOW())
    </select>

    <insert id="insert" parameterType="com.ruoyi.crm.common.domain.CrmIdempotencyKey">
        INSERT INTO crm_idempotency_key (
            id, tenant_id, idempotency_key, request_hash, response_status, response_body, expire_time, create_time
        ) VALUES (
            #{id}, #{tenantId}, #{idempotencyKey}, #{requestHash}, #{responseStatus}, #{responseBody}, #{expireTime}, NOW()
        )
    </insert>

    <update id="updateResponse">
        UPDATE crm_idempotency_key
        SET response_status = #{responseStatus},
            response_body = #{responseBody}
        WHERE id = #{id} AND tenant_id = #{tenantId}
    </update>

    <delete id="deleteExpired">
        DELETE FROM crm_idempotency_key
        WHERE expire_time &lt; NOW()
    </delete>

</mapper>
